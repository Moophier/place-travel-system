# 创作卡片设计规范 (Design.md)

> 项目：文迹 WENJI · 文学巡礼坐标
> 作品：《挪威的森林》— 村上春树
> 版本：v1.0 | 2026-05-14

---

## 1. 设计理念

**核心气质**："翻开一本旧书的褶皱"

页面像一本被反复翻阅的平装书——纸张微微泛黄、油墨略带沉郁、边角有折痕但不破损。所有视觉决策服务于一个核心感受：**这不是一个旅游网站，这是一篇用空间写成的文学批评。**

### 三重隐喻

| 隐喻层 | 视觉映射 | 情感指向 |
|--------|---------|---------|
| **森林** | 深墨底色 + 斑驳噪点 + 纵向流动感 | 迷失、孤独、寻找 |
| **纸张** | 暖米色卡片 + 纤维纹理 + 柔和阴影 | 记忆、书写、典藏 |
| **黄金** | 细线、点缀字、分割装饰 | 珍贵、永恒、不可替代 |

---

## 2. 色彩系统

| 变量名 | 色值 | 用途 | 情感语义 |
|--------|------|------|---------|
| `--gold` | `#c9a84c` | 主装饰色、标题、分割线、图标 | 珍贵、典藏、温暖 |
| `--gold-light` | `#e8d5a3` | 辅助高亮、hover态、渐变端点 | 晨曦、微光 |
| `--gold-dim` | `rgba(201,168,76,0.25)` | 背景装饰、未激活态、边框 | 含蓄、隐约 |
| `--ink` | `#1a1714` | 页面底色 | 深夜、沉静、沉浸 |
| `--paper` | `#f5ede0` | 卡片底色、内容区块 | 信纸、稿纸、留白 |
| `--paper-mid` | `#ede0cc` | 卡片内部分割、引用区块底色 | 纸张阴影、层次 |
| `--text` | `#2c2118` | 正文文字 | 墨色、可读、温暖 |
| `--text-dim` | `rgba(44,33,24,0.55)` | 次要文字、元信息 | 褪色、回忆 |
| `--moss` | `#4a5e3a` | 强调文字、标签、关键信息 | 苔藓、森林、生命力 |
| `--dusk` | `#3a3254` | 备用强调色 | 黄昏、过渡 |

**色彩使用规则：**
1. 底色永远用 `--ink`，不用纯黑 `#000`——保留手工纸张的温暖暗度
2. 金色仅用于装饰和语义标记，不可大面积填充；正文禁止使用金色
3. `--moss` 仅用于 `<strong>` 和 `.tag`——它是森林的颜色，也是思想的颜色
4. 所有半透明色值使用 `rgba`，不用 `opacity`，避免子元素继承

---

## 3. 字体层级

| 层级 | 英文/拉丁 | 中文 | 用途 | 字重 |
|------|-----------|------|------|------|
| **标题** | `Cinzel` | `Noto Serif SC` | 主标题、章节名 | 400 / 900 |
| **正文** | `IM Fell English` | `Noto Serif SC` | 引用、描述文字 | italic / 400 |
| **标注** | `Cinzel` | `Noto Serif SC` | 元信息、标签、小字 | 400 |

**字号阶梯：**

| 语义 | 尺寸范围 | 行高 | 字间距 |
|------|---------|------|--------|
| 主标题（日文） | `clamp(2.2rem, 5vw, 3rem)` | 1.1 | 0.05em |
| 主标题（英文） | `1.05rem` | 1.3 | 0.12em |
| 副标题/品牌 | `0.72rem` | 1.4 | 0.35–0.45em |
| 卡片标题 | `1.4rem`（移动端 `1.2rem`） | 1.2 | 0.04em |
| 正文 | `0.82rem` | 1.85 | 0.02em |
| 引用 | `0.88rem` | 1.75 | 默认 |
| 元信息/标注 | `0.72rem` | 1.5 | 0.08–0.1em |
| 标签 | `0.6rem` | 1.2 | 0.05em |
| 索引编号 | `0.58rem` | 1.2 | 0.35em |

---

## 4. 页面结构 · 六大区块

### 4.1 首页（Hero）
- 品牌副标题 `Literary Footprints · 文学巡礼`
- 日文主标题「挪威的森林」+ 英文 `Norwegian Wood`
- 黄金渐变分割线（180px，两端渐变透明）
- 名言引用 + CTA 按钮
- 底部滚动提示动画

### 4.2 作者档案（Author Block）
- 左侧 4px 渐变金色侧边条（`#c9a84c → #c47d3a`）
- 卡片背景 `#f5ede0`，box-shadow `0 6px 48px rgba(0,0,0,.45)`
- 姓名行中文 `1.7rem Bold`，英文 `italic 1rem`
- 标签 pills：`background: var(--paper-mid)` + 绿色文字

### 4.3 路线导航点（Route Dots）
- 5 个圆点代表 5 个巡礼站点
- 圆点 `10×10px`，边框 `1.5px solid var(--gold)`
- 激活态填充金色 + 发光阴影
- 圆点间 `36px × 1px` 渐变线连接

### 4.4 景点卡片（Site Cards）⭐ 核心组件

**卡片条纹颜色映射：**

| 主题 | 类名 | 渐变色值 | 语义 |
|------|------|---------|------|
| 大学 | `card-stripe-univ` | `#8a6a3a → #5a4020` | 棕褐，学术、古旧 |
| 自然/公园 | `card-stripe-forest` | `#4a7a4a → #2d5c2d` | 森林绿 |
| 生死/墓地 | `card-stripe-death` | `#5a5070 → #362d4a` | 暗紫，肃穆 |
| 音乐/酒吧 | `card-stripe-music` | `#c9a84c → #8a6030` | 金色，爵士 |
| 雪/冬 | `card-stripe-snow` | `#7a8c9a → #4a606e` | 冷灰蓝 |

**卡片内部结构：**
```
索引编号 · Day X · 时段
——————————————————————————
中文地名
推荐季节标签
📍 地址 · 交通方式

"引用文字"
—— 出处说明

描述正文（strong 用绿色）

⏱ 建议时长  [标签] [标签] [标签]
```

**交互规范：**
- hover 时 `translateY(-3px)` + 阴影加深，**无颜色变化**
- 出场动画 `fadeUp` 从 `translateY(20px)` 淡入，每张卡片延迟 0.1s 递增

### 4.5 事实声明（Truth Note）
- 背景 `rgba(201,168,76,0.07)`，极淡金色提示
- 边框 `1px solid var(--gold-dim)`
- `<strong>` 标签使用金色

### 4.6 路线汇总（Route Summary）
- 网格布局：`grid-template-columns: 1.6rem 1fr auto`
- DAY 标题右对齐，交通方式斜体色灰
- 每行间 `1px solid var(--paper-mid)` 分隔

### 4.7 页脚（Footer）
- 品牌名 `Cinzel 1rem` + 字母间距 `0.3em`
- 说明文字 `0.62rem` + `opacity: 0.4`

---

## 5. 全局效果

### 5.1 胶片噪点
SVG fractalNoise 叠加，opacity 0.4，z-index 100

### 5.2 亮色主题
暗底亮卡模式：深墨底色 + 暖白卡片

### 5.3 圆角与边框
- 卡片圆角统一 `4px`（不用大圆角，保留书页直角感）
- 边框极低透明度，仅作结构标记

### 5.4 阴影
- 默认：`0 4px 40px rgba(0,0,0,0.5)`
- hover：`0 12px 60px rgba(0,0,0,0.55)`

---

## 6. 交互规范

| 交互 | 行为 |
|------|------|
| 卡片 hover | translateY(-3px) + 阴影加深，无颜色变化 |
| 页面滚动 | IntersectionObserver 入场动画 |
| 标签 | 纯展示态 |
| 引用区块 | 左侧金色竖线 3px |
| 导航锚点 | 滚动时自动高亮当前章节 |

---

## 7. 响应式断点

```css
@media (max-width: 600px) {
  .masthead-title-jp { font-size: 2.2rem; }
  .card-body { padding: 1.25rem 1.2rem; }
  .card-title-cn { font-size: 1.2rem; }
}
```

---

## 8. 巡礼站点卡片模板

每张卡片创作时需按此模板填写，放入 HTML 的 `.cards-wrap` 容器中：

```html
<div class="site-card" data-site="N">
  <div class="card-row">
    <div class="card-stripe card-stripe-THEME"></div>
    <div class="card-body">
      <div class="card-index">NN · Day X · 时段</div>
      <h3 class="card-title-cn">地名</h3>
      <span class="card-season-badge">标签</span>
      <div class="card-location">📍 地址</div>
      <div class="card-quote">
        <p class="card-quote-text">"引用"</p>
        <p class="card-quote-note">—— 出处</p>
      </div>
      <p class="card-desc">正文 <strong>关键词</strong> 继续</p>
      <div class="card-bottom">
        <span class="card-duration">⏱ 时长</span>
        <div class="card-tags"><span class="tag">标签</span></div>
      </div>
    </div>
  </div>
</div>
```

---

## 9. 平台核心机制（V2.0）

### 首发坐标权
每个文学坐标只允许一人拥有「原创认证」，后来者只能以「继创」存在。

### 三层积分体系
- **购买分**：基础权重
- **出行验证分**：购买分的 3 倍，需真实地理位置
- **共鸣分**：同行评审，权重低但有文化背书

### 供应商边界
- 坐标关联制，每个坐标最多两家供应商
- 创造者拥有一票否决权

### 巡礼创作者身份
出行验证超过 3 次 → 自动获得「巡礼创作者」标记 → 「作者已亲历」

---

*本文件遵循新文人主义暗金调设计范式。*